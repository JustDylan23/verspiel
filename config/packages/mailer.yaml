framework:
    mailer:
        dsn: '%env(MAILER_DSN)%'
        envelope:
            sender: '%env(MAILER_ENVELOPE_SENDER)%'
        headers:
            From: 'Verspiel <%env(MAILER_ENVELOPE_SENDER)%>'
            X-Auto-Response-Suppress: 'OOF, DR, RN, NRN, AutoReply'
